import React from 'react';
import DemoDialogComponent from '@/components/DemoDialog/index';

const DemoDialog = (props) => {
  return <DemoDialogComponent {...props} />;
};

export default DemoDialog;